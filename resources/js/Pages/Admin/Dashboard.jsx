export default function DashboardAdmin(props) {
    return (
        <>
            <div>
                <p>{JSON.stringify(props)}</p>
                <p>Halo ini dashboard admin</p>
            </div>
        </>
    );
}
